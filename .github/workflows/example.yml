name: demo
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Checkout
        uses: actions/checkout@v2
      - run: yarn install
      - name: Verify Microservice One
        run: yarn workspace one verify
  Second-Job:
    runs-on: ubuntu-latest
    needs: Build
    steps:
      - run: echo "second job"
  Third-Job:
    runs-on: ubuntu-latest
    needs: Second-Job
    if: ${{github.ref == 'refs/heads/main'}}
    steps:
      - run: echo "third job"